name: MyProject
user_paths:
- name: MyUserPath
  actions:
    steps:
    - request:
        url: http://www.neotys.com/select?name:neoload
        assertions:
        - name: legacy_assertion
          contains: legacy
        content_assertions:
        - name: new_assertion
          contains: new
