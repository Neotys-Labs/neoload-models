name: MyProject
user_paths:
- name: user_path_1
  actions:
    steps:
    - try_catch:
        name: my_try_catch
        caught_exceptions: timeouts
        try:
          name: container
          steps:
          - delay: 1s
