name: MyProject
user_paths:
- name: MyUserPath
  actions:
    steps:
    - if:
        conditions:
        - "'${variable}' == '2'"
        - "'operand1' == 'operand2'"
        then:
          steps:
          - delay: 3m 200ms
