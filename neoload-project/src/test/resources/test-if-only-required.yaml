name: MyProject
user_paths:
  - name: MyUserPath
    actions:
      steps:
        - if:
            conditions: ["'operand1' equals \"l'arbre\""]
            then:
              steps:
                - delay: 3m 200ms