name: MyProject
user_paths:
- name: MyUserPath
  actions:
    steps:
    - request:
        url: http://www.neotys.com/select?name:neoload
        assertions:
        - name: duplicate
          contains: foo
        - name: duplicate
          contains: bar
