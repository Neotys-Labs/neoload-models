name: MyProject
user_paths:
- name: MyUserPath
  actions:
    steps:
    - transaction:
        name: MyTransaction
        steps:
        - delay: "1000"
        assertions:
        - contains: MyUserPath_actions_MyTransaction
