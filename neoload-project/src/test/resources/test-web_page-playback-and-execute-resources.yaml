name: MyProject
user_paths:
- name: MyUserPath
  actions:
    steps:
    - web_page:
        playback: sequential
        execute_resources: dynamic
        steps:
        - request:
            url: http://host:80/
